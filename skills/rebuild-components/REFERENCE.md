# Rebuild reference

Choose scene before details. Use intentional type, color, spacing, and layout. Motion uses transform/opacity; preserve interrupted states and reduced motion. Review keyboard, focus, contrast, and responsive behavior. Reject generic UI slop.
