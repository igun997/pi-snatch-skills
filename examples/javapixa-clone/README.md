# Static reference rebuild

Serve locally:

```sh
npx serve . -l 4173
```

Open `http://127.0.0.1:4173`.

No target source code, assets, or copied copy. This demo uses newly authored HTML, CSS, JavaScript, text, and CSS-only visual placeholders.
